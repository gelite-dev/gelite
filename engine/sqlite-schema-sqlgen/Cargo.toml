[package]
name = "sqlite-schema-sqlgen"
version.workspace = true
edition.workspace = true

[dependencies]
sqlite-schema-plan = { path = "../sqlite-schema-plan" }

[dev-dependencies]
schema-model = { path = "../schema-model" }
