[package]
name = "query-resolver"
version.workspace = true
edition.workspace = true

[dependencies]
schema-model = { path = "../schema-model" }
query-ast = { path = "../query-ast" }
query-ir = { path = "../query-ir" }
