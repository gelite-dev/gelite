[package]
name = "sqlite-query-sqlgen"
version.workspace = true
edition.workspace = true

[dependencies]
sqlite-query-plan = { path = "../sqlite-query-plan" }

[dev-dependencies]
query-ir = { path = "../query-ir" }
schema-model = { path = "../schema-model" }
