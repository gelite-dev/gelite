//! WASM SQLite runner backend placeholder.
//!
//! This module intentionally does not implement `SQLiteRunner` yet. The first
//! WASM gate is compiling `sqlite-runner` for the target without native SQLite
//! linkage.

pub struct WasmSQLiteRunner;
